server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/noetipo/asistencia-microservicios.git
          searchPaths: config-data
          username: noetipo
          password: Tipo47259697
          default-label: main


  security:
    user:
      name: root
      password: s3cr3t
